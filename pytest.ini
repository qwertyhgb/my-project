[pytest]
# 根目录运行 `pytest -q` 时只收集 tests/，绝不扫描 data/workdir/outputs/third_party/logs。
testpaths = tests
norecursedirs = data workdir outputs third_party logs .git .ruff_cache .pytest_cache __pycache__ build dist *.egg-info
addopts = -q
