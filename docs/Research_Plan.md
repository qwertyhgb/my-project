# Research Plan

## 基于前列腺解剖分区条件的多序列 MRI 空间可靠性融合

**英文工作标题：** *Anatomy-Conditioned Spatial Reliability Fusion of Multi-sequence MRI for Prostate Cancer Lesion Segmentation*

## 1. 文档定位

本文档只讨论研究本身：研究对象、科学问题、相关研究、理论动机、方法机制、可解释性、创新边界与预期学术贡献。

本文档不包含数据划分、模型训练安排、评价指标、对照方案、运行命令、时间表、阶段门或工程实施步骤。代码变化记录在 `Development_Log.md`，已经发生的训练与验证事实记录在 `Training_Log.md`。

---

## 2. 研究主题与核心命题

本研究关注前列腺双参数磁共振成像（biparametric MRI，bpMRI）中的临床显著前列腺癌病灶分割。输入由三个彼此对齐、但成像机理和表观特征不同的 MRI 序列组成：

- T2-weighted MRI（T2W）；
- apparent diffusion coefficient map（ADC）；
- high-b-value diffusion-weighted MRI（HBV）。

研究引入外周带（peripheral zone，PZ）与移行带（transition zone，TZ）作为解剖上下文，核心命题是：

> PZ/TZ 解剖信息不必作为额外病灶证据直接交给分割骨干，而可以专门用于回答“当前位置应如何组合 T2W、ADC 与 HBV”，从而形成解剖条件化、空间变化且连续可学习的多序列融合。

这一定义把研究对象从宽泛的“设计一个更复杂的前列腺分割网络”收窄为一个可解释的机制问题：

> **解剖上下文能否改变模型对不同 MRI 序列的局部相对偏好？**

研究创新集中在分割骨干之前的轻量门控层。病灶分割的通用能力继续由成熟的原生 nnU-Net 提供，避免把骨干变化与融合机制混为一谈。

---

## 3. 医学与成像学动机

### 3.1 三个 MRI 序列并非可互换通道

T2W、ADC 与 HBV 来源于不同的成像对比机制，它们对前列腺结构和可疑病灶的表达具有互补性：

- **T2W** 更直接地呈现腺体形态、内部结构、包膜及组织学相关的信号差异，是理解前列腺解剖背景的重要来源；
- **ADC** 描述水分子表观扩散水平，为受限扩散提供定量或半定量表征；
- **HBV** 强调高 b 值下的扩散受限高信号，但也更容易受噪声、伪影和空间分辨率限制影响。

因此，“三个序列都存在”不等于“所有位置都应同等使用三个序列”。序列价值可能随病例、扫描质量、病灶表型和空间位置变化。多序列融合的关键并不只是增加输入，而是决定在何处保留、抑制或增强各序列信号。

### 3.2 PZ 与 TZ 提供不同的组织背景

PZ 与 TZ 具有不同的组织构成、正常表观和常见良性改变。PI-RADS v2.1 的判读框架也明确体现了区域相关性：PZ 的主导序列是 DWI，而 TZ 的主导序列是 T2W；但 PI-RADS 是影像判读框架，并不是体素级病灶分割的硬编码规则。[ACR PI-RADS v2.1](https://cs.acr.org/-/media/ACR/Files/RADS/PI-RADS/PIRADS-V2-1.pdf)

这一医学事实支持“序列作用可能具有解剖依赖性”，却不能推出以下过强结论：

- PZ 内只能使用扩散序列；
- TZ 内只能使用 T2W；
- 同一分区内部的所有体素应采用相同权重；
- 人工判读中的主导序列规则可以直接等价为神经网络融合规则。

病灶可能位于分区边界、跨越多个分区，或呈现不典型信号；分区本身也可能存在误差和模糊边界。因此，本研究采用连续、数据驱动的软条件化，而不是按区域硬切换序列。

### 3.3 解剖分区本身是可建模但不完美的先验

既往研究已经证明深度学习可以从前列腺 MRI 中分割 PZ 与 TZ，但 PZ 边界通常比整个腺体或 TZ 更难稳定确定。这说明分区先验具有实际可获得性，同时也必须被视为带误差的辅助信息，而非绝对真值。[Bardis et al., 2021](https://doi.org/10.1148/rycan.2021200024)

因此，理想机制应满足两点：

1. 能利用 PZ/TZ 提供的结构上下文；
2. 不允许 PZ/TZ 绕过影像内容，直接决定病灶输出。

本研究通过“分区只进入门控、分割骨干只接收 MRI”来表达这一边界。

### 3.4 PI-CAI 提供的任务背景

PI-CAI 将 T2W、ADC 与高 b 值扩散 MRI 上的临床显著前列腺癌检测作为公开研究任务，并表明基于 bpMRI 的人工智能系统已经可以在大规模、多中心条件下形成有竞争力的病灶判断。[Saha et al., 2024](https://doi.org/10.1016/S1470-2045(24)00220-1)

PI-CAI 官方基线采用 nnU-Net 作为病灶建模基础，也说明本研究没有必要重新发明完整分割框架；真正值得研究的是如何在强基线之上表达序列间关系。[PI-CAI official nnU-Net baseline](https://github.com/DIAGNijmegen/picai_baseline/blob/main/nnunet_baseline.md)

本研究与 PI-CAI 的关系是任务和方法背景关系，而不是把挑战框架本身作为创新：研究仍然集中在解剖条件化融合机制。

---

## 4. 相关研究脉络

### 4.1 强分割骨干：nnU-Net

nnU-Net 的核心贡献不是某一个新的注意力模块，而是根据任务属性自动配置预处理、网络拓扑、patch 尺寸、训练与推理流程，从而建立强而稳定的医学图像分割基线。[Isensee et al., 2021](https://doi.org/10.1038/s41592-020-01008-z)

后续的 nnU-Net Revisited 进一步指出：许多看似先进的网络结构，在强基线、资源规模和验证条件得到充分控制后，并不稳定优于配置良好的卷积 U-Net。这为本研究提供了重要的方法论约束：研究问题是“解剖信息如何改变多序列融合”，没有必要为了体现创新而重新设计完整编码器和解码器。[Isensee et al., 2024](https://arxiv.org/abs/2404.09556)

因此，原生 nnU-Net 在本研究中承担三个角色：

- 提供成熟的病灶表征和密集预测能力；
- 固定通用分割机制，使研究变量集中在输入融合；
- 防止把复杂骨干带来的容量变化误写成解剖条件门控的贡献。

### 4.2 从通道拼接到多流融合

最直接的多序列处理方式是把 T2W、ADC、HBV 沿通道维拼接，再交给同一个编码器。其优点是结构简单、计算开销低；局限是序列交互由后续卷积隐式承担，模型没有显式表示“哪个序列在什么位置更值得依赖”。

多流方法为每个序列建立独立或部分独立的特征路径，再在特征层进行融合。Jiang 等提出的 MSFusion-UNet 使用多流编码器，并在不同层通过空间注意图加权各序列卷积输出，说明前列腺病灶分割中的模态作用可以被显式建模为随空间变化的权重。[Jiang et al., 2023](https://doi.org/10.1002/mp.16374)

这类方法建立了两个重要前提：

- 多序列不是必须等权融合；
- 权重不必是每个病例一个全局标量，可以是空间分布图。

但多流编码器同时改变了特征提取方式、参数规模和融合位置。若研究目标只关注“解剖是否应影响序列权重”，完整多流网络会引入不必要的结构变量。

### 4.3 模态信息量与自适应可靠性

NaMa 针对前列腺多模态 MRI 提出 modality-informativeness adaptive learning，通过序列间相关性估计病例相关的模态信息量，并将其用于自适应特征融合。这项工作说明，不同病例中最有用的序列组合可能不同，也说明“学习模态权重”本身已经是既有研究方向。[Meng et al., 2024](https://doi.org/10.1609/aaai.v38i5.28215)

因此，本研究不能把以下内容单独宣称为创新：

- 使用注意力机制；
- 学习三个 MRI 序列的权重；
- 根据影像内容进行动态融合；
- 生成空间变化的模态权重图。

本研究真正需要回答的是更窄的问题：**当影像驱动的动态融合已经存在时，PZ/TZ 是否应作为条件变量直接改变这一融合函数？**

### 4.4 解剖先验直接作为网络输入

Karagoz 等先用分区网络获得概率性前列腺分区图，再将 T2W、ADC、高 b 值 DWI 与分区图共同输入 3D nnU-Net。这一路线说明，PZ/TZ 可以作为附加通道向病灶模型提供有用的空间上下文。[Karagoz et al., 2023](https://doi.org/10.1186/s13244-023-01439-0)

直接拼接分区通道的机制边界并不清晰：解剖信息可以参与编码器中的任意计算，既可能影响序列融合，也可能充当位置先验、前列腺范围提示或直接病灶判别特征。即使最终表现发生变化，也很难只归因于“解剖改变了模态可靠性”。

本研究与该路线的关键差异是：

- PZ/TZ 只参与三个 MRI 序列尺度系数的生成；
- PZ/TZ 不作为额外通道进入 nnU-Net 分割骨干；
- 解剖信息的功能被限制为“调节如何看 MRI”，而不是“直接告诉网络在哪里分割”。

### 4.5 解剖感知结构与分区损失

AtPCa-Net 同时使用 T2W、ADC、高 b 值 DWI、TZ 和 PZ 掩膜，并通过对称感知结构与 zonal loss 表达不同分区中的病灶表观差异。它表明解剖知识可以进入网络结构、标签设计和目标函数，而不只是作为后处理规则。[Zheng et al., 2024](https://doi.org/10.1038/s41598-024-56405-7)

AtPCa-Net 关注的是更广义的解剖感知病灶检测，包括对称性、层级标签和分区约束；本研究则把问题限制在一个更小的机制单元：**PZ/TZ 是否改变三序列的局部相对权重**。二者共享解剖动机，但研究变量不同。

### 4.6 条件调制与注意力的概念来源

FiLM 证明条件信息可以通过逐特征仿射变换调制主干特征；Attention U-Net 则证明轻量注意门可以在保持 U-Net 主体的情况下选择性突出相关空间区域。[Perez et al., 2018](https://doi.org/10.1609/aaai.v32i1.11671)；[Oktay et al., 2018](https://arxiv.org/abs/1804.03999)

本研究借鉴的是“条件信息控制主信息流”的一般思想，但机制并不等同于 FiLM 或 Attention U-Net：

- 条件变量是 PZ/TZ；
- 被调制对象是三个 MRI 序列，而不是任意中间特征；
- 输出是三个非负、相互竞争的尺度系数；
- 门控发生在原生分割骨干之前；
- 研究对象是跨序列相对偏好，而不是单一空间显著性。

### 4.7 文献综合后的研究缺口

相关研究已经分别建立了以下事实与方法基础：

1. bpMRI 的多个序列具有互补信息；
2. 前列腺病灶分割可以使用多流或注意机制进行自适应融合；
3. PZ/TZ 可以作为附加输入、概率掩膜、结构约束或分区损失；
4. 轻量条件调制可以控制网络中的特征流；
5. nnU-Net 可以作为无需重新发明的强分割骨干。

在本次定向调研覆盖的文献中，仍未被直接回答的机制问题是：

> 在不让分区先验直接进入分割骨干、也不采用硬分区选序列规则的前提下，PZ/TZ 能否专门条件化 T2W、ADC、HBV 的空间相对权重？

这是当前研究的精确定位。该表述不等于“首次提出”，也不排除系统检索范围之外存在相近方法。任何进一步的新颖性主张都必须建立在可复核的系统文献检索之上。

### 4.8 相关工作的机制对照

| 研究方向或代表工作 | 核心机制 | PZ/TZ 的角色 | 是否显式学习序列权重 | 与本研究的主要差异 |
|---|---|---|---|---|
| nnU-Net | 自配置的强分割框架 | 无特定分区机制 | 否 | 本研究把它作为固定骨干，不把骨干本身作为创新 |
| MSFusion-UNet | 多流编码与分层空间注意融合 | 未专门条件化 PZ/TZ | 是 | 使用完整多流特征编码；本研究采用骨干前的最小输入门控 |
| NaMa | 病例相关的模态信息量与序列间相关性 | 未把 PZ/TZ 作为核心条件 | 是 | 证明自适应序列融合已有；本研究追问解剖是否提供增量条件信息 |
| Karagoz 等 | MRI 与概率分区掩膜共同输入 3D nnU-Net | 普通附加输入 | 不单独约束为序列权重 | 分区可参与骨干任意计算；本研究限制其只能改变 MRI 尺度 |
| AtPCa-Net | 对称感知结构、分区输入与 zonal loss | 输入、标签和目标函数约束 | 不是核心研究对象 | 研究广义解剖感知；本研究隔离“解剖条件序列融合” |
| 本研究 | PZ/TZ 条件化的三序列空间尺度场 | 只进入门控 | 是 | PZ/TZ 不进入分割骨干，研究变量更集中 |

该表用于界定机制差异，不构成优劣排序，也不替代更大范围的系统综述。

### 4.9 本次调研的范围与限制

本次内容优化依据截至 2026-09-21 可检索到的官方指南、原始研究论文、官方数据资源与官方代码说明，重点覆盖：

- PI-RADS 的分区相关判读依据；
- nnU-Net 与强分割基线方法论；
- 前列腺多序列自适应融合；
- PZ/TZ 分割与解剖感知病灶建模；
- 条件调制和轻量注意门控。

这是一轮围绕当前方法问题的定向调研，不是符合 PRISMA 等规范的系统综述。因此，文档只提出受限的方法定位，不作穷尽性或首创性声明。

---

## 5. 概念与术语定义

### 5.1 多序列，而非多模态临床数据

T2W、ADC 与 HBV 都属于 MRI。本研究统一称为“多序列 MRI 融合”。“模态权重”只作为机器学习领域常见表达使用，不意味着三个输入来自三种独立医疗设备或三类临床数据。

### 5.2 本研究中“可靠性”的操作性定义

本研究没有逐体素的真实可靠性标签，因此门控系数不是经过校准的正确概率，也不是图像质量评分。

“空间可靠性”在本文中只表示：

> 模型为了完成病灶表征而学习到的、随空间位置变化的 MRI 序列相对缩放系数。

更严格的论文用语应优先采用：

- learned sequence preference；
- relative modality weighting；
- spatial sequence scaling；
- anatomy-conditioned fusion coefficient。

除非未来建立独立的可靠性监督与校准定义，否则不应把系数称为：

- 某序列“真实可靠度”；
- 某序列的“贡献百分比”；
- 某序列对诊断的“因果重要性”；
- 放射科医师的判读权重。

### 5.3 解剖先验

PZ/TZ 通道表示分区归属或重采样后的分区占比。即使数值位于 0 到 1 之间，也不自动等于经过校准的分区概率。

本研究把它们视为有信息但可能有误差的条件变量：

- 可以影响融合；
- 不直接决定病灶；
- 不被解释为绝对解剖真值；
- 不允许替代原始 MRI 内容。

### 5.4 空间门控

“空间”表示每个体素位置都可以具有不同的三个序列系数，而不是整个病例共享一个固定权重。门控网络虽然使用 1×1×1 卷积，但归一化层会利用当前特征图的统计量，因此其输出应被理解为一个由整块输入条件化的空间权重场，而不是彼此完全独立的逐体素查表函数。

---

## 6. 核心研究问题

### RQ1：多序列相对作用是否需要空间建模？

T2W、ADC、HBV 的有用信息是否随局部组织背景、病灶表型和图像质量变化？如果需要，这种变化能否由连续空间权重表达，而不是被隐藏在普通卷积通道中？

### RQ2：影像内容本身能否形成有意义的序列偏好？

在不提供 PZ/TZ 的情况下，门控函数能否根据三个 MRI 序列自身形成非均匀、非静态但稳定的相对权重？这一问题用于区分“动态融合本身”与“解剖条件带来的额外变化”。

### RQ3：PZ/TZ 是否对序列偏好提供增量条件信息？

在 MRI 内容保持不变的概念条件下，加入或改变 PZ/TZ 是否会系统性改变三个序列的局部相对权重？如果不会，说明解剖通道没有真正参与融合判断；如果变化完全由分区决定，则说明模型可能退化为硬位置先验。

### RQ4：软条件化能否表达分区内部差异与边界过渡？

模型能否在同一分区内部根据影像内容产生不同权重，并在 PZ/TZ 边界或跨区病灶处保持连续变化？这决定了解剖条件化是否超越简单的区域查表。

### RQ5：最小门控是否足以承载研究问题？

如果一个很小的输入级门控已经能够表达解剖相关的序列偏好，就没有必要同时修改编码器、解码器、skip connection 或损失结构。研究需要判断复杂度是否来自科学问题本身，而不是模型设计惯性。

### RQ6：门控图能够支持多强的机制解释？

门控图是否只反映优化后形成的内部缩放，还是能够稳定呈现空间选择性、序列竞争和解剖敏感性？无论观察到何种结构，都必须区分“模型行为描述”与“临床或因果解释”。

---

## 7. 科学假设与可证伪含义

### H1：空间异质性假设

三个 MRI 序列的相对作用不是全局常数，而是随位置和影像内容变化。

如果学习到的权重几乎处处相同、只表现为病例级常数，或始终固定偏向单一序列，则该假设不能得到机制层面的支持。

### H2：影像驱动可靠性假设

只根据 T2W、ADC、HBV，轻量门控就可以形成与局部影像模式相关的序列偏好，而不必依赖完整多流编码器。

如果权重始终保持均匀，或变化主要来自无结构噪声，则“影像驱动的空间可靠性”解释不成立。

### H3：解剖增量条件假设

PZ/TZ 在 MRI 内容之外提供与融合决策相关的结构上下文，使门控权重对相同或相近的影像模式产生区域相关调节。

如果加入 PZ/TZ 后权重函数没有可辨认变化，则解剖条件未被利用；如果权重只复制 PZ/TZ 轮廓，则模型更可能学习了位置捷径，而非影像与解剖的联合判断。

### H4：软条件化假设

连续权重比按 PZ/TZ 预设固定序列更符合病灶异质性，因为它允许：

- 同一区域内存在不同序列偏好；
- 边界位置平滑过渡；
- 影像证据在必要时覆盖区域先验；
- 每个病例形成不同的融合模式。

如果权重在每个分区内退化为固定模板，软条件化的主要价值就没有体现。

### H5：最小扰动假设

将研究模块限制在原生 nnU-Net 之前，并从恒等映射开始，可以让模型首先保留成熟三序列表示，再逐步学习相对缩放。这样产生的变化更容易归因于融合机制，而不是骨干重构。

### H6：有限解释假设

门控权重可以描述模型内部的相对序列偏好，但不足以单独证明临床重要性、真实图像质量或因果贡献。任何超过“模型内部行为”的解释都需要额外证据。

---

## 8. 方法的数学定义

以下公式统一使用 `$$ ... $$` 块级语法，以兼容常见 Markdown 数学渲染器。每个公式后均给出纯文本解释，即使编辑器不渲染公式也不影响阅读。

### 8.1 输入、空间域与预测目标

设空间域为：

$$
\Omega = \{1,\ldots,D\} \times \{1,\ldots,H\} \times \{1,\ldots,W\}.
$$

三个 MRI 序列组成输入张量：

$$
X = [X_{T2W}, X_{ADC}, X_{HBV}] \in \mathbb{R}^{3 \times D \times H \times W}.
$$

PZ/TZ 解剖先验组成条件张量：

$$
Z = [Z_{PZ}, Z_{TZ}] \in [0,1]^{2 \times D \times H \times W}.
$$

目标是得到病灶概率场：

$$
\widehat{Y} \in [0,1]^{D \times H \times W}.
$$

纯文本解释：`X` 有 3 个 MRI 通道，`Z` 有 2 个解剖通道，输出 `Y_hat` 是每个体素的病灶概率。

### 8.2 两种条件信息边界

影像条件门控只观察 MRI：

$$
C_{img} = X.
$$

解剖条件门控同时观察 MRI 与裁剪到有效范围的 PZ/TZ：

$$
C_{ana} = \operatorname{Concat}\left(X,\operatorname{Clip}(Z,0,1)\right).
$$

纯文本解释：影像条件输入有 3 个通道；解剖条件输入有 5 个通道。裁剪只保证分区数值落在 0 到 1 之间，不把它们宣称为校准概率。

### 8.3 轻量空间门控

门控函数由两层 1×1×1 卷积、归一化和非线性组成：

$$
U = \sigma\left(\operatorname{IN}\left(\operatorname{Conv}_{1\times1\times1}^{(1)}(C)\right)\right),
$$

$$
A = \operatorname{Conv}_{1\times1\times1}^{(2)}(U).
$$

其中：

- `C` 是 `C_img` 或 `C_ana`；
- `IN` 表示 instance normalization；
- `sigma` 表示 LeakyReLU；
- `A` 有 3 个通道，分别对应 T2W、ADC、HBV 的未归一化门控 logit。

该门控不提取一个新的分割表示，只产生三个 MRI 序列的尺度场。

### 8.4 相对偏好与尺度系数

对三个序列 logit 做通道方向 softmax：

$$
W_m(p) = \frac{\exp(A_m(p))}{\sum_{j=1}^{3}\exp(A_j(p))},
\qquad m \in \{1,2,3\},\ p \in \Omega.
$$

于是：

$$
W_m(p) \ge 0,
\qquad
\sum_{m=1}^{3} W_m(p) = 1.
$$

`W` 表示归一化的相对序列偏好。为使均匀偏好对应恒等缩放，定义实际尺度：

$$
S_m(p) = 3W_m(p).
$$

因此：

$$
S_m(p) \ge 0,
\qquad
\sum_{m=1}^{3} S_m(p) = 3.
$$

纯文本解释：三个偏好 `W` 的和为 1；实际尺度 `S` 的和为 3。若三个序列等权，则每个 `W=1/3`，每个 `S=1`。

### 8.5 MRI 加权与分割骨干

门控后的 MRI 为：

$$
X'_m(p) = S_m(p)X_m(p).
$$

三个加权 MRI 通道进入原生 nnU-Net：

$$
\widehat{Y} = F_{\theta}(X').
$$

其中 `F_theta` 表示 nnU-Net 分割函数。关键结构约束是：

$$
Z \notin \operatorname{Input}(F_{\theta}).
$$

纯文本解释：PZ/TZ 可以影响 `S`，但 nnU-Net 只看到被加权后的 T2W、ADC、HBV，看不到 PZ/TZ 本身。

### 8.6 恒等初始化

第二个 1×1×1 卷积的权重和偏置初始化为 0，因此初始 logit 为：

$$
A_m(p) = 0.
$$

由此得到：

$$
W_m(p) = \frac{1}{3},
\qquad
S_m(p) = 1,
\qquad
X'_m(p) = X_m(p).
$$

纯文本解释：模型开始时不会放大或缩小任何 MRI 序列；门控网络初始等价于不存在，之后才学习偏离恒等状态。

### 8.7 序列竞争的数学含义

softmax 尺度对 logit 的局部导数为：

$$
\frac{\partial S_m}{\partial A_k}
= 3W_m\left(\delta_{mk}-W_k\right).
$$

其中 `delta_mk` 是 Kronecker delta：当 `m=k` 时为 1，否则为 0。

该式说明三个序列不是彼此独立放大：提高一个序列的 logit 会增加其自身相对尺度，同时通过归一化降低其他序列的相对份额。因此，这个门控表达的是**相对竞争关系**，不是三个互不相关的质量分数。

### 8.8 软条件化与硬路由的区别

硬路由可以抽象为：

$$
m^*(p) = h(Z(p)),
$$

其中分区直接决定唯一序列 `m*`。这种规则忽略同一区域内部的影像差异，也容易在边界产生不连续选择。

本研究的软条件化是：

$$
W(p) = G_{\phi}(X,Z)(p).
$$

纯文本解释：PZ/TZ 只是门控函数的条件之一，最终权重仍由 MRI 和解剖信息共同决定；三个序列都可以保留非零权重。

---

## 9. 研究机制的逐层推理

### 9.1 为什么不满足于普通通道拼接？

普通拼接并非错误，它能让卷积网络学习跨通道特征。但其跨序列关系分散在大量卷积核中，不提供明确的相对权重对象，也无法直接区分以下两种机制：

- 网络改变了病灶特征提取；
- 网络改变了对各 MRI 序列的依赖方式。

显式门控把第二种机制单独表示出来，使“如何融合”成为可观察的研究对象。

### 9.2 为什么门控放在骨干之前？

输入级门控具有明确的最小性：

- 每个尺度通道与一个 MRI 序列一一对应；
- 加权结果可以直接解释为该序列输入被相对放大或抑制；
- nnU-Net 内部结构保持不变；
- 不需要复制三套编码器；
- 不引入多尺度、跨层或 decoder 侧的额外注意模块。

其代价是表达能力弱于深层特征融合：门控只能调节输入强度，不能直接选择复杂语义特征。这一限制不是缺陷掩饰，而是研究设计的一部分——先回答最小机制是否成立，再决定是否需要更复杂的特征级条件化。

### 9.3 为什么 PZ/TZ 不直接进入骨干？

若 PZ/TZ 与 MRI 一起进入骨干，网络可能以多种方式利用分区：

- 作为前列腺范围提示；
- 作为病灶位置先验；
- 作为普通纹理通道；
- 作为抑制腺体外假阳性的掩膜；
- 作为序列融合条件。

这些作用无法自然分离。本研究通过结构限制，把允许的路径压缩为：

$$
Z \longrightarrow S \longrightarrow X' \longrightarrow \widehat{Y}.
$$

禁止直接路径：

$$
Z \nrightarrow F_{\theta}.
$$

纯文本解释：解剖信息只能先改变 MRI 尺度，再间接影响分割；不能直接进入 nnU-Net。

### 9.4 为什么使用 PZ/TZ，而不使用 WG？

whole-gland（WG）主要表达腺体内外范围，不能区分 PZ 与 TZ。把 WG 加入门控容易使研究问题从“分区相关的序列偏好”漂移为“利用前列腺 ROI 抑制背景”。这可能有工程价值，但不是当前研究命题。

因此，WG 不进入门控，也不进入分割骨干。它不属于核心条件变量。

### 9.5 为什么采用和为 3 的正尺度？

若直接使用 softmax 权重乘 MRI，均匀状态会把每个序列缩小到原来的三分之一。后续网络虽然可能适应这一变化，但门控初始状态不再与无门控输入等价。

乘以序列数 3 后，均匀状态变为 `[1,1,1]`，带来三个性质：

- 恒等初态；
- 总尺度守恒；
- 权重变化主要表达序列间重新分配，而不是整体增益变化。

### 9.6 为什么不把门控权重称为概率？

softmax 只保证非负且和为 1，并不自动赋予概率校准含义。这里没有“某序列在该体素正确”的随机事件标签，`W` 只是优化目标下形成的归一化系数。

因此：

$$
W_m(p) \neq P(\text{sequence }m\text{ is clinically correct at }p).
$$

纯文本解释：`W` 是模型偏好，不是真实可靠性的概率。

---

## 10. 具体研究内容

### 10.1 多序列互补性的机制表达

研究 T2W、ADC、HBV 的互补性如何从“共同输入”转化为“可观察的相对序列偏好”。重点不是证明多序列通常比单序列包含更多信息，而是明确模型是否需要在不同位置重新分配注意对象。

需要区分三类可能状态：

1. **均匀融合：** 三个序列始终接近等权，说明显式选择可能没有必要；
2. **全局偏置：** 某个序列在几乎所有位置占优，说明门控主要学习了数据集级平均偏好；
3. **空间选择：** 权重随局部结构、病灶和成像质量变化，才符合空间可靠性的研究设想。

### 10.2 影像驱动与解剖条件驱动的概念分解

将融合函数分为两个层次理解：

$$
W_{img} = G_{img}(X),
$$

$$
W_{ana} = G_{ana}(X,Z).
$$

`W_img` 表示 MRI 自身能够支持的空间偏好；`W_ana` 表示加入解剖上下文后的条件偏好。研究关注的不是单独观察 `W_ana`，而是理解解剖信息是否带来超出影像内容的、具有结构意义的变化。

### 10.3 解剖信息究竟调节什么？

PZ/TZ 可能通过以下方式影响门控：

- 改变相同信号模式的区域解释；
- 在组织背景不同的位置调整 T2W 与扩散序列的相对优先级；
- 在分区边界提供连续上下文；
- 帮助门控区分病灶样信号与区域特异的正常或良性表现。

这些是机制假设，而不是预先写死的规则。模型不应被强制要求在 PZ 提高某一个固定序列，也不应被强制要求在 TZ 提高另一个固定序列。

### 10.4 分区内部异质性

如果解剖条件化只产生两张固定模板——一张对应 PZ，一张对应 TZ——那么模型只是把硬规则换成了可学习查表。真正的条件融合应允许：

- 同一 PZ 内不同体素具有不同权重；
- 同一 TZ 内不同病例具有不同权重；
- 病灶附近与正常组织的权重模式不同；
- 解剖相同但影像质量不同的区域产生不同响应。

因此，研究对象是 `X` 与 `Z` 的交互，而不是 `Z` 对权重的单独映射。

### 10.5 边界与跨区病灶

PZ/TZ 边界是检验软条件化概念合理性的关键区域。离散分区在重采样后可能形成 0 到 1 之间的占比值，这些值更适合解释为边界体素中的区域占比，而不是分类置信度。

门控应允许边界处形成连续权重，不要求病灶被唯一归入某个分区。跨区病灶的融合决策仍由 MRI 内容与局部解剖共同产生。

### 10.6 解剖先验误差与模型行为

PZ/TZ 可能出现轮廓偏差、局部缺失、边界不稳或来源差异。因为先验直接进入门控，错误分区可能造成错误的序列缩放。

研究上需要区分两种相反风险：

- **先验失效：** 模型完全忽略 PZ/TZ，解剖条件名存实亡；
- **先验支配：** 模型机械复制分区结构，即使 MRI 内容不支持也改变权重。

理想状态位于二者之间：分区提供上下文，MRI 保留最终主导权。

### 10.7 轻量性与研究可归因性

门控模块只负责生成三个尺度场，不承担病灶特征编码。轻量性在这里不是单纯追求更少参数，而是为了维持清晰的因果图式：

$$
\text{anatomy context}
\longrightarrow
\text{sequence scaling}
\longrightarrow
\text{MRI representation}
\longrightarrow
\text{lesion segmentation}.
$$

结构越复杂，可替代解释越多；结构越小，越容易明确研究结论究竟涉及哪个机制。

---

## 11. 门控图的机制解释框架

### 11.1 可解释对象一：权重均匀度

可以用归一化熵描述三个序列偏好的均匀程度：

$$
H(p) = -\frac{1}{\log 3}\sum_{m=1}^{3}W_m(p)\log\left(W_m(p)+\epsilon\right).
$$

其概念范围为 0 到 1：

- `H` 接近 1：三个序列接近均匀；
- `H` 较低：门控更集中于少数序列。

熵只能描述选择性强弱，不能说明选择是否正确。

### 11.2 可解释对象二：解剖响应

可以概念性地定义解剖条件响应：

$$
R_Z(p) = \frac{1}{2}
\sum_{m=1}^{3}
\left|
W_m(X,Z;p)-W_m(X,Z_0;p)
\right|.
$$

其中 `Z_0` 表示不携带分区信息的中性条件。`R_Z` 描述同一 MRI 在有无解剖条件时权重变化的大小。

这个量回答“模型是否响应解剖信息”，但仍不能回答“这种响应是否具有临床正确性”。

### 11.3 可解释对象三：空间变化

对每个序列权重场，可用空间梯度表示局部变化：

$$
V_m(p) = \left\|\nabla W_m(p)\right\|_2.
$$

较大的 `V_m` 表示权重在附近快速改变。它可能对应组织边界、病灶边缘、伪影或噪声，因此不能仅凭空间变化就赋予医学意义。

### 11.4 可以作出的解释

在证据充分时，可以描述：

- 模型在不同位置形成了不同的序列偏好；
- 解剖条件改变了门控图；
- 某些区域的权重更均匀或更集中；
- 门控对分区边界、病灶附近或伪影区域表现出特定响应；
- 权重模式在不同病例之间存在变化。

### 11.5 不可以仅凭门控图作出的解释

不能仅凭权重图断言：

- 某个序列在临床上更重要；
- 模型使用了与放射科医师相同的推理；
- 权重更高的序列对最终输出具有更大的因果贡献；
- PZ/TZ 条件改善了患者结局或临床决策；
- 权重图等同于病灶显著图、质量图或不确定性图。

softmax 权重存在相互依赖，输入归一化也会影响尺度含义。门控图必须作为模型内部状态理解，而不是自动升级为生物标志物。

---

## 12. 研究创新的准确边界

### 12.1 可以主张的方法定位

本研究可以准确表述为：

> 在原生 nnU-Net 之前，以 PZ/TZ 为条件生成 T2W、ADC、HBV 的逐体素相对尺度，并只把加权后的 MRI 送入分割骨干，从而把解剖信息的功能限定为多序列融合调节。

其组合特征包括：

- 解剖条件化；
- 三序列相对竞争；
- 空间连续权重；
- 输入级轻量门控；
- 恒等初始化；
- 分区先验与分割骨干隔离；
- nnU-Net-first 的最小扩展。

### 12.2 不能单独作为创新的内容

以下思想已有充分邻近工作，不能单独作为创新声明：

- 使用 U-Net 或 nnU-Net；
- 使用注意力或门控；
- 多序列 MRI 输入；
- 学习模态权重；
- 生成空间注意图；
- 使用 PZ/TZ 掩膜；
- 把解剖信息加入前列腺癌模型；
- 使用 softmax 归一化权重。

### 12.3 新颖性表述纪律

当前可以使用的谨慎表述是：

> 本研究聚焦于一种机制上更受限的解剖条件融合：PZ/TZ 不直接参与病灶分割，只调节三序列 MRI 的空间相对尺度。

在完成系统文献检索之前，不使用“首次”“首个”“唯一”或“填补空白”等绝对表述。定向调研只能帮助明确邻近工作，不能替代系统综述。

---

## 13. 主要理论风险与替代解释

### 13.1 权重塌缩为全局序列偏置

门控可能只学到数据集平均偏好，例如长期偏向扩散序列，而没有真正形成空间变化。此时模型仍可能工作，但不能支持“空间可靠性”这一机制解释。

### 13.2 权重复制解剖掩膜

模型可能把 PZ/TZ 当作查表索引，在每个分区输出近似固定权重。这只能说明分区与权重相关，不能说明模型联合利用了 MRI 内容。

### 13.3 解剖位置捷径

若病灶分布在不同分区中不均衡，PZ/TZ 可能被模型当作病灶先验，而不是融合条件。虽然分区不直接进入骨干降低了这一风险，但通过序列缩放仍可能间接传递位置模式。

### 13.4 先验误差传播

错误的 PZ/TZ 会改变序列尺度，并可能放大原始图像中的不利信号。对先验进行 0 到 1 裁剪只能限制数值范围，不能纠正语义错误。

### 13.5 序列配准误差

空间门控假设同一体素位置上的 T2W、ADC、HBV 与 PZ/TZ 具有足够一致的解剖对应。若序列之间存在错位，门控可能把不同组织位置的信息放在一起比较，从而削弱“局部可靠性”的含义。

### 13.6 输入强度尺度影响

门控直接读取预处理后的 MRI 强度。不同归一化方式会改变门控看到的数值分布，因此权重既反映生物与成像信息，也可能反映预处理尺度。门控系数不能脱离输入定义单独解释。

### 13.7 softmax 的零和竞争

三个偏好之和固定为 1，一个序列权重上升必然伴随至少一个其他序列下降。该机制擅长表达相对选择，却不能表达“三个序列在某区域同时都不可靠”或“所有序列同时需要整体增强”。

### 13.8 输入级门控的表达上限

输入级缩放无法完成复杂的跨序列语义对齐，也无法选择深层特征。若研究问题实际需要高级语义交互，轻量门控可能表达不足。但在证明这种必要性之前，不应预先扩张到多分支或多尺度复杂结构。

### 13.9 权重不可辨识性

后续 nnU-Net 可以通过卷积参数补偿输入尺度变化。不同的门控与骨干参数组合可能产生相近输出，因此权重并非唯一机制分解。权重图应被看作一种可观察内部表示，而不是唯一解释。

---

## 14. 研究范围与非目标

### 14.1 研究范围

- 前列腺 bpMRI 中的临床显著病灶分割；
- T2W、ADC、HBV 三序列的空间相对融合；
- PZ/TZ 作为门控条件；
- 轻量输入级可靠性门控；
- 原生 nnU-Net 作为固定分割骨干；
- 门控权重的机制层解释与解释边界。

### 14.2 明确非目标

- 不重新设计完整 U-Net、Transformer 或 Mamba 骨干；
- 不复制 nnU-Net 的编码器、解码器或通用训练机制；
- 不训练新的 PZ/TZ 或 WG 分割网络作为本研究创新；
- 不进行病灶与解剖分区的联合多任务学习；
- 不用 WG 作为病灶模型输入；
- 不设计按分区硬切换序列的规则系统；
- 不把权重解释为放射科诊断规则；
- 不把内部权重直接解释为临床获益、因果贡献或不确定性；
- 不把普通注意力、PZ/TZ 输入或 nnU-Net 本身包装为新颖贡献。

---

## 15. 研究逻辑链

本研究的完整推理链条如下：

1. T2W、ADC、HBV 提供不同但互补的前列腺 MRI 信息；
2. 不同序列的有用程度可能随病例和空间位置变化；
3. 因此，多序列关系适合由空间相对权重显式表达；
4. PZ 与 TZ 提供与组织背景有关的结构上下文；
5. 但分区不应被硬编码为固定选序列规则；
6. 因此，PZ/TZ 应作为软条件变量，与 MRI 共同决定权重；
7. 为了隔离这一机制，PZ/TZ 只进入门控，不进入分割骨干；
8. 为了避免重构骨干造成混杂，加权后的 MRI 交给原生 nnU-Net；
9. 为了保留已有输入行为，门控从严格恒等映射开始；
10. 最终研究贡献应落在“解剖如何条件化多序列融合”，而不是复杂网络堆叠。

可压缩为一条概念路径：

$$
(X,Z)
\longrightarrow
\text{anatomy-conditioned sequence gate}
\longrightarrow
S
\longrightarrow
X'
\longrightarrow
\text{native nnU-Net}
\longrightarrow
\widehat{Y}.
$$

---

## 16. 预期学术贡献

### 16.1 方法贡献

提出一个位于原生 nnU-Net 之前的轻量解剖条件序列门控，把 PZ/TZ 的作用严格限制为调节 T2W、ADC、HBV 的空间相对尺度。

### 16.2 概念贡献

区分三类容易混淆的解剖利用方式：

- 解剖作为普通附加输入；
- 解剖作为病灶位置或目标函数约束；
- 解剖作为多序列融合函数的条件变量。

本研究专注第三类。

### 16.3 方法论贡献

以强、成熟、原生的 nnU-Net 作为分割基础，把创新限制在一个最小模块中，减少因自写训练框架、完整骨干替换或多模块叠加产生的归因歧义。

### 16.4 可解释性贡献

建立一套克制的门控解释框架：可以讨论空间偏好、序列竞争和解剖响应，但明确拒绝把权重直接解释为临床重要性、正确概率或因果贡献。

### 16.5 研究边界贡献

即使最终不支持解剖条件假设，也能形成有意义的科学结论：

- 输入级门控是否足以表达多序列关系；
- PZ/TZ 是否提供超出 MRI 的融合上下文；
- 轻量条件化何时退化为全局偏置或位置捷径；
- 何时才有理由引入更复杂的特征级融合。

---

## 17. 参考研究

1. Turkbey B, Rosenkrantz AB, Haider MA, et al. **Prostate Imaging Reporting and Data System Version 2.1: 2019 Update of Prostate Imaging Reporting and Data System Version 2.** *European Urology*, 2019. [ACR PI-RADS v2.1 PDF](https://cs.acr.org/-/media/ACR/Files/RADS/PI-RADS/PIRADS-V2-1.pdf)
2. Isensee F, Jaeger PF, Kohl SAA, Petersen J, Maier-Hein KH. **nnU-Net: a self-configuring method for deep learning-based biomedical image segmentation.** *Nature Methods*, 2021. [DOI](https://doi.org/10.1038/s41592-020-01008-z)
3. Isensee F, Wald T, Ulrich C, et al. **nnU-Net Revisited: A Call for Rigorous Validation in 3D Medical Image Segmentation.** 2024. [arXiv](https://arxiv.org/abs/2404.09556)
4. Saha A, Bosma JS, Twilt JJ, et al. **Artificial intelligence and radiologists in prostate cancer detection on MRI (PI-CAI): an international, paired, non-inferiority, confirmatory study.** *The Lancet Oncology*, 2024. [DOI](https://doi.org/10.1016/S1470-2045(24)00220-1)
5. Saha A, Twilt JJ, Bosma JS, et al. **The PI-CAI Challenge: Public Training and Development Dataset.** [Zenodo](https://zenodo.org/records/6517398)
6. Jiang M, Yuan B, Kou W, et al. **Prostate cancer segmentation from MRI by a multistream fusion encoder.** *Medical Physics*, 2023. [DOI](https://doi.org/10.1002/mp.16374)
7. Meng R, Zhang X, Huang S, et al. **NaMa: Neighbor-Aware Multi-Modal Adaptive Learning for Prostate Tumor Segmentation on Anisotropic MR Images.** *AAAI*, 2024. [DOI](https://doi.org/10.1609/aaai.v38i5.28215)
8. Karagoz A, Alis D, Seker ME, et al. **Anatomically guided self-adapting deep neural network for clinically significant prostate cancer detection on bi-parametric MRI: a multi-center study.** 2023. [DOI](https://doi.org/10.1186/s13244-023-01439-0)
9. Zheng H, et al. **AtPCa-Net: anatomical-aware prostate cancer detection network on multi-parametric MRI.** *Scientific Reports*, 2024. [DOI](https://doi.org/10.1038/s41598-024-56405-7)
10. Bardis M, Houshyar R, Chantaduly C, et al. **Segmentation of the Prostate Transition Zone and Peripheral Zone on MR Images with Deep Learning.** *Radiology: Imaging Cancer*, 2021. [DOI](https://doi.org/10.1148/rycan.2021200024)
11. Perez E, Strub F, de Vries H, Dumoulin V, Courville A. **FiLM: Visual Reasoning with a General Conditioning Layer.** *AAAI*, 2018. [DOI](https://doi.org/10.1609/aaai.v32i1.11671)
12. Oktay O, Schlemper J, Le Folgoc L, et al. **Attention U-Net: Learning Where to Look for the Pancreas.** 2018. [arXiv](https://arxiv.org/abs/1804.03999)
13. DIAGNijmegen. **PI-CAI nnU-Net baseline documentation.** [Official baseline repository](https://github.com/DIAGNijmegen/picai_baseline/blob/main/nnunet_baseline.md)

---

## 18. 一句话总结

> 本研究不是再造一个分割网络，而是研究一个明确机制：让 PZ/TZ 只负责调节 T2W、ADC、HBV 在不同空间位置上的相对使用方式，再由原生 nnU-Net 完成病灶分割。
