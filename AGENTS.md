# 项目协作规则

本文件适用于 `/opt/data/private/lm/my-projects` 及其全部子目录。除用户在当前对话中明确覆盖外，所有协作代理必须遵守以下规则。

## 1. 长任务必须显示进度

- 数据清点、转换、重采样、预处理、审计、推理、评估和模型训练必须默认显示可观察的进度条。
- 优先使用 `tqdm` 或等价实现，至少显示当前数量、总数量、完成比例、处理速率和预计剩余时间。
- 训练应显示 epoch 进度；必要时显示 batch 进度。分布式训练只由主进程显示进度，避免重复输出。
- 进度条不能替代结构化日志。任务结束后仍须输出成功数、失败数、跳过数、耗时和输出路径。
- 新增或修改相关脚本时，必须检查进度条是否覆盖主要耗时循环；默认开启，可提供显式 `--no-progress` 选项用于日志环境。

## 2. 数据处理、训练与长时命令由用户运行

- 代理可以阅读文件、检查代码、修改脚本和文档，并执行轻量、非破坏、非数据处理的秒级诊断命令（读取配置、解析 plan/split、`--help`、静态与语法检查等）。
- 代理不得自行启动数据转换、重采样、预处理、全量数据审计、数据集物化、批量推理、模型评估或模型训练。
- **除数据处理与训练外，验证（validation / verify）、推理、评测、批量分析等任何预计耗时较长的命令，同样不得由代理自行启动，必须整理为命令交由用户运行。**
- 上述命令必须以可直接复制的形式提供给用户，由用户决定何时运行。
- 代理不得因为命令规模较小或使用 `--limit`、smoke test、单病例、`--epochs 1` 等参数而自行执行数据处理；除非用户在当前对话中明确授权代理运行该具体命令。
- 代理可以运行不读取真实医学数据的单元测试、静态检查和语法检查；若测试会处理真实数据、生成大规模派生文件或启动训练或推理，同样必须交给用户运行。
- 给出运行命令时必须注明工作目录、conda 环境（统一为 `lm`）及必要的 `source scripts/env_nnunet.sh`、预期输出、进度观察方式和成功判据。
- 用户返回日志或产物后，代理负责检查结果、定位问题并给出下一条命令。
- 命令归属不明确时，默认按「交给用户运行」处理。

## 3. 数据安全

- 原始医学数据保持只读。
- 不静默删除、覆盖或排除病例。
- 派生数据写入项目约定的 `data/interim/`、`data/processed/` 或 `outputs/`。
- 任何可能覆盖已有派生结果的命令，都必须在交给用户前明确说明影响与恢复方式。

## 4. 运行环境与依赖

### 4.1 始终使用 conda 环境 `lm`

- 所有 Python、pytest、数据处理、训练、推理与评测命令一律在 conda 环境 **`lm`**（`/root/anaconda3/envs/lm`）下运行，禁止使用 `base` 或其他环境。
- 执行前先激活：`conda activate lm`。
- 涉及 nnU-Net 的命令还需先执行 `cd /opt/data/private/lm/my-projects && source scripts/env_nnunet.sh`（该脚本会校验 `lm` 环境与固定版本源码，并用项目内 `nnUNet_*` 路径覆盖 shell 全局变量）。
- 代理在执行任何命令前必须先确认当前 Python 是 `/root/anaconda3/envs/lm/bin/python`；不是则先激活 `lm` 再执行。
- 交付给用户的命令同样必须显式写明 `conda activate lm`（必要时附 `source scripts/env_nnunet.sh`）。

### 4.2 缺失的包由代理自行安装到 `lm`

- 发现缺少 Python 包时，代理应自行安装到 `lm` 环境，不得因缺包中止任务或要求用户代为安装。
- 安装方式固定为：`conda activate lm` 后执行 `python -m pip install <package>`，或 `conda install -n lm <package>`；禁止安装到 `base` 或用户目录。
- 安装后必须验证导入与版本（例如 `python -c "import X; print(X.__version__)"`），并在交付说明中记录新增包及其版本。
- 未经用户确认，不得升级或降级可能破坏既有依赖的包（如 `nnunetv2`、`torch`、`numpy`、`SimpleITK`）；遇到版本冲突时先停止，说明冲突与候选方案后再由用户决定。
- 环境变更属于可追溯信息：重要实验开始前，新增依赖需记录到运行说明或实验日志中。
